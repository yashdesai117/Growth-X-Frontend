/**
 * src/app/(auth)/login/page.tsx  [STUB]
 *
 * Login page — renders the LoginForm component.
 * Route: /login
 */

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign In — GrowthX AI",
  description: "Sign in to your GrowthX AI account.",
};

export default function LoginPage() {
  return (
    <main>
      <h1>Sign In</h1>
      {/* TODO (Session 02+): Render <LoginForm /> here */}
      <p>Login form stub — implement in Session 02</p>
    </main>
  );
}
