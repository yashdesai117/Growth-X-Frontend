/**
 * src/app/(dashboard)/overview/page.tsx
 *
 * Dashboard overview — placeholder for Session 08.
 * Session 07 scope: just needs to exist so redirect after login works.
 */

export default function OverviewPage() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <div className="text-center">
        <h1 className="text-2xl font-semibold text-white mb-2">Dashboard</h1>
        <p className="text-zinc-500">Session 08 will build this page.</p>
      </div>
    </div>
  );
}
